import sp from './spacing';
import globalStyle from './global-style';
import { darkTheme, lightTheme } from './themes';

const themes = {
  dark: darkTheme,
  light: lightTheme,
};

export { sp, globalStyle, themes };
